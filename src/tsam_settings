[SERVER]
Your server's IP:
ip=127.0.0.1

Server's query port:
port=10011

Virtual server's ID (usually 1 unless multiple servers present):
sid=1

[QUERY DATA]
login=serveradmin
password=admin

[SETTINGS]
Delay between snapshots in seconds:
delay-seconds=3600

Removes snapshots older than value in days:
old-time=62

Peak hours start time:
peak-start=14 

Peak hours end time:
peak-end=21  

Create snapshots only during these hours:
record-start=07

record-end=00

Ignore tracking temporary channels (incl. semi-permament)
ignore-temp=1
